;; This is used to test the ScriptEditorDB class.  Since this class records
;; line numbers, changing line numbers in this file requires changing test
;; suites to match.

(define (foo)
  1)

(define (add x y)
  (+ x y))
