The files in this directory are used with a program called DialogBlocks to
generate the ../src/tamale.xrc file.  If we decide not to use DialogBlocks,
we can always delete this directory and use another xrc editor.